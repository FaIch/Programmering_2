package edu.ntnu.idatt2001.wargamesjfx.battle;

/**
 * The enum Terrain.
 * Used for calculating bonuses based on which terrain the battle is happening in
 */
public enum Terrain {
    /**
     * Forest terrain.
     */
    Forest,
    /**
     * Hill terrain.
     */
    Hill,
    /**
     * Plains terrain.
     */
    Plains
}
